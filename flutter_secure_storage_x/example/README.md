# flutter_secure_storage_x Example

Demonstrates how to use the `flutter_secure_storage_x` plugin.

## Running the Example

You can run this example application on a simulator or device.

## Integration Tests

To run the integration tests for this package, run the following command from this directory (`flutter_secure_storage_x/example`):

```shell
flutter test integration_test
```